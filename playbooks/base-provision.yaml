---

- hosts: myhost 
  gather_facts: yes
  tasks:
    - name: install python 3
      raw: test -e /usr/bin/python || (apt -y update && apt install -y python)
  become: yes


- hosts: myhost
  roles:
    - base-provision
  become: yes
